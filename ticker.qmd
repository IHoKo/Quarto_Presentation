---
pagetitle: "Ticker · Building with AI"
format:
  revealjs:
    theme: [dark, theme/ticker.scss]
    width: 1600
    height: 900
    margin: 0.06
    slide-number: c/t
    transition: slide
    transition-speed: fast
    background-transition: fade
    hash: true
    menu: true
    preview-links: false
    highlight-style: github-dark
    html-math-method: plain
    footer: "Ticker · one person · nine hours · ticker-dash.fly.dev"
    include-in-header: theme/plotly-head.html
    include-after-body: theme/reveal-fixes.js
    mermaid:
      theme: dark
jupyter: python3
execute:
  echo: false
  warning: false
  freeze: auto
---

```{python}
#| label: setup

import numpy as np
import pandas as pd
import plotly.graph_objects as go
import plotly.io as pio
from IPython.display import HTML, display

# Design tokens, taken verbatim from the live app's stylesheet so the deck
# and the product read as one system.
PAPER = "#0e1116"
INK   = "#f5f2ec"
SLATE = "#1a2028"
GAIN  = "#2f7d6e"
LOSS  = "#a8442f"
BRASS = "#c9a227"
MUTED = "#8b929c"
GRID  = "#232a33"

# ---------------------------------------------------------------------------
# EVIDENCE.  Every number below is measured from the Ticker repository or
# quoted from its own planning documents. Nothing here is invented.
#
#   git log            21 commits
#   plan.md   Sec.10   per-phase estimates, written before any code existed
#   prompts.md         "The plan is 8-12 days of work"
#   cloc-equivalent    line counts by extension, node_modules excluded
# ---------------------------------------------------------------------------

PY_LINES, TS_LINES = 7963, 3033
TOTAL_LINES = PY_LINES + TS_LINES          # 10,996 -> "about 11,000"
TESTS = 291

# plan.md Sec.10 build order: (phase, low, high) in developer-days.
# Labels are the plain-English discipline, not the phase name: a mixed
# audience reads "Deployment" far faster than "Phase 6 - polish & harden".
PHASES = [
    ("Phase 0 · skeleton",        0.5, 0.5),
    ("Phase 1 · data in",         1.0, 1.0),
    ("Phase 2 · live prices",     1.0, 2.0),
    ("Phase 3 · portfolio",       2.0, 2.0),
    ("Phase 4 · charts",          1.0, 1.0),
    ("Phase 5 · chat",            2.0, 3.0),
    ("Phase 6 · polish & harden", 1.0, 2.0),
]
PLAN_LO  = sum(p[1] for p in PHASES)          # 8.5 days
PLAN_HI  = sum(p[2] for p in PHASES)          # 11.5 days
PLAN_MID = (PLAN_LO + PLAN_HI) / 2            # 10.0 days

# Hands-on time, reconstructed from commit timestamps: gaps longer than
# 45 minutes are treated as time away and excluded, plus 30 minutes of
# lead-in per session. That gave 9.2 h; rounded DOWN to 9 so the claim sits
# at the conservative end of the measurement.
ACTUAL_H = 9.0
HOURS_PER_DAY = 8.0                           # one developer-day
PLAN_LO_H, PLAN_HI_H = PLAN_LO * HOURS_PER_DAY, PLAN_HI * HOURS_PER_DAY
PLAN_MID_H = PLAN_MID * HOURS_PER_DAY

SPEEDUP_LO  = PLAN_LO_H / ACTUAL_H            # 7.6x
SPEEDUP_HI  = PLAN_HI_H / ACTUAL_H            # 10.2x
SPEEDUP_MID = PLAN_MID_H / ACTUAL_H           # 8.9x
REDUCTION   = 1 - ACTUAL_H / PLAN_MID_H

# Chart type is sized for a projector, not a laptop: base 20px, ticks 17px.
# Nothing on a chart is allowed to be smaller than the deck's caption text.
pio.templates["ticker"] = go.layout.Template(
    layout=dict(
        paper_bgcolor="rgba(0,0,0,0)",
        plot_bgcolor="rgba(0,0,0,0)",
        font=dict(family="Instrument Sans, ui-sans-serif, system-ui", color=INK, size=20),
        colorway=[BRASS, GAIN, LOSS, MUTED],
        xaxis=dict(gridcolor=GRID, zerolinecolor=GRID, linecolor=GRID,
                   tickfont=dict(family="IBM Plex Mono, monospace", size=17, color=MUTED)),
        yaxis=dict(gridcolor=GRID, zerolinecolor=GRID, linecolor=GRID,
                   tickfont=dict(family="IBM Plex Mono, monospace", size=17, color=MUTED)),
        showlegend=False,
        margin=dict(l=64, r=28, t=44, b=56),
        hoverlabel=dict(bgcolor=SLATE, bordercolor=BRASS,
                        font=dict(family="IBM Plex Mono, monospace", size=17, color=INK)),
    )
)
pio.templates.default = "ticker"
rng = np.random.default_rng(20260907)
CONFIG = {"displayModeBar": False, "responsive": True}

def show(fig):
    """Emit a chart as plain HTML; plotly.js is loaded once from <head>."""
    height = fig.layout.height or 500
    display(HTML(pio.to_html(
        fig, include_plotlyjs=False, full_html=False, config=CONFIG,
        default_width="100%", default_height=f"{height}px")))

def menu_style(**kw):
    """Chart buttons, sized to be clickable and legible from a lectern."""
    base = dict(
        type="buttons", direction="right",
        bgcolor=SLATE, bordercolor=GRID, borderwidth=1,
        font=dict(family="IBM Plex Mono, monospace", size=16, color=INK),
        pad=dict(r=10, t=10), showactive=False, active=0,
        x=0, xanchor="left", y=1.18, yanchor="top",
    )
    base.update(kw)
    return base
```

##  {#title .center}

::: {style="max-width:27em"}
[What AI-assisted development actually looks like · September 2026]{.kicker}

::: {style="font-family:'Bricolage Grotesque',sans-serif;font-weight:700;font-size:4.6em;line-height:0.95;letter-spacing:-0.04em;margin:0.1em 0 0 0"}
Ticker
:::

::: {.title-lead}
A live stock dashboard, built by one person.

**From a written idea to a tested application running on the internet — in nine hours.**
:::

::: {style="margin-top:1.2em;font-family:'IBM Plex Mono',monospace;font-size:0.44em;letter-spacing:0.14em;text-transform:uppercase;color:#8b929c"}
AAPL [319.97]{data-tick="319.97" style="color:#2f7d6e"} &nbsp;·&nbsp;
MSFT [499.70]{data-tick="499.70" style="color:#2f7d6e"} &nbsp;·&nbsp;
NVDA [230.36]{data-tick="230.36" style="color:#a8442f"} &nbsp;·&nbsp;
ticker-dash.fly.dev
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:45 — TOTAL 0:45]</span>

Right — my slot this month. I'll start with a question rather than a status update.

How much working software do you think one person can build in about a day of effort?

<span style="color:#c1121f;font-weight:700">[PAUSE — LET THEM ANSWER IT IN THEIR HEAD]</span>

Not a mock-up. A real application — live data, a database, a proper interface, deployed and running right now.

A couple of years ago my answer would have been: very little.

So I ran the experiment on myself. This is Ticker. Nine hours of hands-on work, from a written description to a tested application anyone can open in a browser.

I'll show you what it does, and then exactly how it got built.
:::

## What I wanted

[One screen · three things]{.kicker}

::: {.cols .cols-3}
::: {.card}
[One]{.tag}[See prices move]{.h4}

Live, as trades happen. Not a page I have to keep refreshing.
:::
::: {.card}
[Two]{.tag}[Know what I actually made]{.h4}

Real profit and loss, worked out properly from everything I've ever bought and sold.
:::
::: {.card}
[Three]{.tag}[Just ask it questions]{.h4}

In plain English. "How am I doing?" — and get an answer grounded in my real numbers.
:::
:::

::: {style="margin-top:1.1em"}
::: {.pull}
Three ordinary-sounding asks. Three completely different engineering problems.
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:40 — TOTAL 1:25]</span>

The idea was deliberately unglamorous. One screen, three things.

<span style="color:#c1121f;font-weight:700">[POINT TO EACH CARD IN TURN — DON'T READ THEM OUT]</span>

Watch prices move as they actually happen. Know exactly what I'd made or lost — the real number, not a rough one. And be able to just ask it things, in normal language.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

Anyone could write those three sentences on the back of an envelope in a minute.

<span style="color:#c1121f;font-weight:700">[SLOW DOWN]</span>

But each one is a completely different engineering problem underneath. Live data is one discipline. Financial accounting is another. A safe AI assistant is a third.

That gap — between how easy something is to describe and how hard it is to build — is what this talk is about.
:::

## What came out

[One machine · three rented services · running right now]{.kicker}

::: {.figrow}
::: {}
[~11,000]{.fig-n}[lines of code]{.fig-t}
:::
::: {}
[291]{.fig-n}[automated tests]{.fig-t}
:::
::: {}
[9]{.fig-n .brass}[hours of work]{.fig-t}
:::
::: {}
[~$3]{.fig-n .brass}[a month to run]{.fig-t}
:::
:::

::: {.cols .cols-3 style="margin-top:1.1em"}
::: {.card}
[Live]{.tag}[Prices that move]{.h4}

One connection to the market, shared out to every browser that's open.
:::
::: {.card}
[Correct]{.tag}[A real ledger]{.h4}

Profit and loss calculated the same way your broker does it for tax.
:::
::: {.card}
[Safe]{.tag}[An analyst that can't trade]{.h4}

It can read everything and suggest a change. It cannot make one.
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:45 — TOTAL 2:10]</span>

This is what came out the other end.

About eleven thousand lines of code. Two hundred and ninety-one automated tests — small programs that check the software still works every time anything changes. Nine hours of work. Three dollars a month to run.

<span style="color:#c1121f;font-weight:700">[POINT TO THE THREE CARDS]</span>

And it does all three things.

Prices move live. The ledger is real — the same accounting method your broker uses to report your gains for tax.

And the analyst can read your holdings, search the web, and suggest a change — but it physically cannot make one. It can only propose. You press Confirm.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

Let me just show you it running.
:::

##  {#live-demo background-iframe="https://ticker-dash.fly.dev/" background-interactive="true"}

[Live · ticker-dash.fly.dev]{.demo-badge}

::: {.demo-overlay}
[If the frame is blank, the session cookie was blocked]{.hint}
[Open live demo ↗](https://ticker-dash.fly.dev/){target="_blank" rel="noopener"}
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 1:50 — TOTAL 4:00]</span>

<span style="color:#c1121f;font-weight:700">[BEFORE THE TALK: LOG IN TO ticker-dash.fly.dev IN ANOTHER TAB OF THIS SAME BROWSER. IF THIS FRAME SHOWS THE PASSPHRASE GATE, CLICK THE GOLD "OPEN LIVE DEMO" BUTTON, BOTTOM RIGHT, AND DEMO IN A REAL TAB.]</span>

<span style="color:#c1121f;font-weight:700">[SHOW APP]</span>

This is the actual application, running on a server right now. You could open it on your phone during this talk.

<span style="color:#c1121f;font-weight:700">[POINT TO THE TICKER TAPE — DON'T CLICK YET]</span>

Across the top is the ticker tape. When the market's open, those numbers flip as real trades happen. Each symbol updates on its own — one price change repaints one row, not the whole screen. That's the hardest problem in the app, and I'll come back to it.

<span style="color:#c1121f;font-weight:700">[SCROLL DOWN TO THE POSITIONS TABLE]</span>

Here's the portfolio. What I paid, what it's worth, whether I'm up or down.

<span style="color:#c1121f;font-weight:700">[POINT TO ANY DASH, OR TO "MARKET CLOSED" IF WE ARE OUT OF HOURS]</span>

And where a price isn't available it shows a dash, not a zero. A zero looks like a real number and quietly lies to you.

<span style="color:#c1121f;font-weight:700">[DEMO — CLICK INTO THE ANALYST PANEL. TYPE: "How is my portfolio doing?" AND SEND.]</span>

Now the analyst.

<span style="color:#c1121f;font-weight:700">[WAIT FOR THE ANSWER TO STREAM IN — ABOUT 5 SECONDS. DON'T TALK OVER IT.]</span>

It's reading my actual holdings — the same real numbers on the screen.

<span style="color:#c1121f;font-weight:700">[DEMO — NOW TYPE: "Add 10 shares of NVDA" AND SEND. WAIT FOR THE CONFIRM CARD.]</span>

Now I'll ask it to change my portfolio.

<span style="color:#c1121f;font-weight:700">[POINT TO THE CONFIRM CARD. DO NOT CLICK CONFIRM.]</span>

It didn't. It's produced a proposal and it's waiting for me. And that isn't politeness — that part of the system has no ability to write to the database at all. It can look; it cannot touch.

<span style="color:#c1121f;font-weight:700">[CLICK DISMISS, THEN RETURN TO THE SLIDES]</span>

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

So — how long should something like that take to build?
:::

## Eighteen specialisms, one small app

[What a project like this normally needs]{.kicker}

::: {.chipgrid}
[Backend APIs]{.chip}[Real-time connections]{.chip}[Live event streams]{.chip}[React interfaces]{.chip}[Client state]{.chip}[TypeScript]{.chip}
[Database design]{.chip}[Schema migrations]{.chip}[FIFO lot accounting]{.chip}[Caching]{.chip}[Rate limiting]{.chip}[Containers]{.chip}
[Automated deployment]{.chip}[Cloud hosting]{.chip}[Login & sessions]{.chip}[Secrets handling]{.chip}[Design systems]{.chip}[Test engineering]{.chip}
:::

::: {style="margin-top:1.1em"}
::: {.pull}
In most organisations, that list isn't one person. It's a team.
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:50 — TOTAL 4:50]</span>

Before we talk about speed, here's what "it" actually involves — and why software is usually slow.

<span style="color:#c1121f;font-weight:700">[GESTURE ACROSS THE WHOLE GRID — DO NOT READ THE ITEMS OUT]</span>

Eighteen separate skills went into that one screen.

I won't read them. But look at the range. Keeping a live connection open to the stock market. Designing a database. The accounting method for working out gains properly. Security. The machinery that gets it onto the internet and keeps it there. Visual design. Testing.

<span style="color:#c1121f;font-weight:700">[PAUSE, THEN SLOW DOWN]</span>

Now read that as a staffing question rather than a technical one.

In most organisations that isn't one person. That's a back-end engineer, a front-end engineer, someone comfortable with data modelling, a designer, and somebody who owns deployment. Five or six specialisms — and five or six calendars.
:::

## The plan said two working weeks

[Estimated phase by phase, before a single line of code existed]{.kicker}

::: {.split}
::: {}
```{python}
#| label: skills

# plan.md Sec.10 gives an estimate per phase. Mapping each phase to the
# discipline that would own it in a conventional team gives the shape of the
# work. Midpoints used where the spec gave a range. Labels are plain English:
# the audience has to read this bar chart in about three seconds.
DISCIPLINES = [
    ("Backend",        3.25),
    ("Frontend",       2.25),
    ("Data & ledger",  1.50),
    ("AI integration", 1.25),
    ("Design",         0.75),
    ("Deployment",     0.50),
    ("Testing",        0.50),
]
labels = [d[0] for d in DISCIPLINES]
days   = [d[1] for d in DISCIPLINES]

fig = go.Figure(go.Bar(
    x=days, y=labels, orientation="h",
    marker=dict(color=MUTED, line=dict(color=PAPER, width=1)),
    text=[f"  {d} days" for d in days], textposition="outside", cliponaxis=False,
    textfont=dict(family="IBM Plex Mono, monospace", size=20, color=INK),
    hovertemplate="<b>%{y}</b><br>%{x} developer-days<extra></extra>"))

fig.update_layout(
    height=580, autosize=True, bargap=0.3,
    xaxis=dict(visible=False, range=[0, 4.6]),
    yaxis=dict(autorange="reversed", showgrid=False, zeroline=False,
               ticklabelstandoff=16,
               tickfont=dict(family="Instrument Sans", size=23, color=INK)),
    margin=dict(l=230, r=120, t=10, b=10))
show(fig)
```
:::

::: {}
::: {style="padding-top:0.6em"}
[8.5–11.5]{.hero}

[**developer-days.** The project's own written estimate, phase by phase — spread across five or six different specialisms.]{.hero-note}
:::

[Source: the project's specification, §10. Written before the build, and never revised afterwards.]{.source}
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:55 — TOTAL 5:45]</span>

Here's where the numbers start, and I want to be careful about where they come from.

Before any code existed, this project had a written specification. The work was broken into seven phases, and each was sized in developer-days — the ordinary way any team estimates work.

<span style="color:#c1121f;font-weight:700">[EMPHASISE]</span>

The important thing is the timing. That estimate is timestamped in the project, it was made before anyone knew how long the build would take, and it was never revised afterwards.

<span style="color:#c1121f;font-weight:700">[POINT TO CHART]</span>

Grouped by the kind of work: the engine room, everything you see, the data and the accounting, the AI, design, deployment, testing.

<span style="color:#c1121f;font-weight:700">[POINT TO THE BIG NUMBER]</span>

Eight and a half to eleven and a half developer-days. Roughly two working weeks — spread across five or six specialisms.

<span style="color:#c1121f;font-weight:700">[IF CHALLENGED ON THIS ESTIMATE — DO NOT READ ALOUD: THE PLAN WAS DRAFTED BY THE SAME TOOL THAT DID THE BUILD. SAY SO PLAINLY IF ASKED. THE SIZING IS CONVENTIONAL, IT IS IN THE REPOSITORY TO BE READ, AND THE AUDIENCE HAS JUST SEEN THE SCOPE. INVITE THEM TO SUBSTITUTE THEIR OWN NUMBER.]</span>

So — what actually happened?
:::

## It took nine hours

[Same scope · same specification · measured after the build]{.kicker}

::: {.split}
::: {}
```{python}
#| label: time-comparison

# Two bars, no axis, no legend, no gridlines. The comparison IS the chart;
# anything else on it is competing with the only thing that matters.
y = ["The plan", "What it took"]
v = [PLAN_MID_H, ACTUAL_H]

fig = go.Figure()
fig.add_trace(go.Bar(
    x=[PLAN_MID_H], y=[y[0]], orientation="h",
    marker=dict(color=MUTED, line=dict(color=PAPER, width=1)),
    error_x=dict(type="data", symmetric=False,
                 array=[PLAN_HI_H - PLAN_MID_H],
                 arrayminus=[PLAN_MID_H - PLAN_LO_H],
                 color=INK, thickness=2, width=12),
    hovertemplate="%{y}<br>%{x:.0f} hours<extra></extra>"))
fig.add_trace(go.Bar(
    x=[ACTUAL_H], y=[y[1]], orientation="h",
    marker=dict(color=BRASS, line=dict(color=PAPER, width=1)),
    hovertemplate="%{y}<br>%{x:.0f} hours<extra></extra>"))

# Figures sit clear of the error bar rather than butting against it, which is
# what `textposition="outside"` would do.
for label, xpos, ypos, colour in (
        (f"{PLAN_LO_H:.0f}–{PLAN_HI_H:.0f} hours", PLAN_HI_H + 6, y[0], INK),
        (f"{ACTUAL_H:.0f} hours",                  ACTUAL_H + 6,  y[1], BRASS)):
    fig.add_annotation(x=xpos, y=ypos, text=f"<b>{label}</b>", showarrow=False,
                       xanchor="left", yanchor="middle",
                       font=dict(family="Bricolage Grotesque", size=38, color=colour))

fig.update_layout(
    height=500, autosize=True, bargap=0.55, barmode="overlay",
    xaxis=dict(visible=False, range=[0, 152]),
    yaxis=dict(categoryorder="array", categoryarray=y[::-1],
               showgrid=False, zeroline=False,
               ticklabelstandoff=16,
               tickfont=dict(family="Instrument Sans", size=26, color=INK)),
    margin=dict(l=290, r=40, t=40, b=20))
show(fig)
```

[The thin line on the grey bar is the estimate's own range. The nine hours counts only hands-on time — any gap over 45 minutes is excluded — and is rounded down.]{.caption}
:::

::: {}
::: {style="padding-top:0.4em"}
[Roughly]{.pre}
[9×]{.hero}

[**faster than the plan.** Around 89% less time — between eight and ten times, depending on which end of the estimate you take.]{.hero-note}
:::
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 1:25 — TOTAL 7:10]</span>

<span style="color:#c1121f;font-weight:700">[SLOW DOWN. THIS IS THE CENTREPIECE SLIDE. DON'T RUSH IT.]</span>

<span style="color:#c1121f;font-weight:700">[POINT TO CHART — THE GREY BAR]</span>

The grey bar is the plan. Sixty-eight to ninety-two hours.

The gold bar is what it actually took.

<span style="color:#c1121f;font-weight:700">[PAUSE — LET THEM SEE THE GAP BEFORE YOU SAY THE NUMBER]</span>

Nine hours.

<span style="color:#c1121f;font-weight:700">[POINT TO THE BIG NUMBER]</span>

About nine times faster. Getting on for ninety percent less time.

Now let me defend that, because a single figure with no working behind it is worth nothing.

<span style="color:#c1121f;font-weight:700">[POINT TO THE THIN LINE ON THE GREY BAR]</span>

The estimate is a range, so I've drawn the range rather than picking the flattering end. Depending where you land, it's between eight and ten times.

And the nine hours is hands-on time only, reconstructed from the project's own history. If I stepped away for more than about three quarters of an hour, I stopped counting. Then I rounded down.

<span style="color:#c1121f;font-weight:700">[EMPHASISE]</span>

So if anything, the comparison is generous to the traditional side.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

And sit with this for a second. That isn't an expert engineer going nine times faster than themselves. It's one person, across eighteen specialisms most of which I had never shipped, ending up with a finished, deployed application anyway.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

So — how? Because it is not typing "build me a stock dashboard" and going for a coffee.
:::

## It started with a plan, not a prompt

[What got written before any code existed]{.kicker}

::: {.split}
::: {}
::: {style="padding-top:0.2em"}
[1,064]{.hero}

[**lines of plain writing** — the architecture, how it should behave when things go wrong, and what "finished" meant. Drafted in a sitting, argued over, then treated as fixed.]{.hero-note}
:::

::: {.figrow .n-3 style="margin-top:1.2em"}
::: {}
[606]{.fig-n}[the plan]{.fig-t}
:::
::: {}
[342]{.fig-n}[decisions made]{.fig-t}
:::
::: {}
[116]{.fig-n}[how to hand it over]{.fig-t}
:::
:::
:::

::: {}
::: {.card}
[prompts.md]{.tag}[The rule that mattered]{.h4}

["Do not paste one giant 'build the whole thing' prompt. The plan is 8–12 days of work; a single request produces a large volume of untested code that all fails together, and you lose the ability to tell which part is wrong. Run one phase per session instead."]{.quote}
:::
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:55 — TOTAL 8:05]</span>

This is the slide I'd most like you to remember.

Nobody opened a chat window and asked for a stock dashboard. The first thing made wasn't code — it was a plan. A thousand lines of ordinary English: what it should do, how it should behave when things go wrong, and what "finished" meant.

<span style="color:#c1121f;font-weight:700">[SLOW DOWN — BE STRAIGHT ABOUT THIS]</span>

And I'll be straight about how it got written. I didn't type all of it. I said what I wanted the product to be, and the plan came back drafted. My job was to read it, push back, and decide when it was settled.

Writing a specification is normally the slow, dreaded part — the thing that stops people ever starting. Here it was an afternoon.

<span style="color:#c1121f;font-weight:700">[POINT TO THE QUOTE — YOU CAN READ THIS ONE OUT]</span>

My favourite line in the project. Don't paste one giant "build the whole thing" prompt — you get a mountain of code that all fails at once, and can't tell which part is wrong.
:::

## One phase at a time

[Seven phases · the same short loop each time]{.kicker}

```{mermaid}
%%{init: {'theme':'base',
  'fontFamily':'Instrument Sans, ui-sans-serif, system-ui, sans-serif',
  'flowchart':{'htmlLabels':true,'useMaxWidth':true,'curve':'basis','nodeSpacing':40,'rankSpacing':78},
  'themeVariables':{
  'background':'#0e1116','primaryColor':'#1a2028','primaryTextColor':'#f5f2ec',
  'primaryBorderColor':'#232a33','lineColor':'#5c6672','fontSize':'21px',
  'edgeLabelBackground':'#0e1116'}}}%%
flowchart LR
  BRIEF["<b>Describe<br/>one piece</b>"]
  BUILD["<b>AI builds it</b><br/>code + tests"]
  CHECK["<b>Machine checks it</b><br/>291 tests run"]
  READ["<b>I read it</b>"]
  SHIP["<b>Ship it</b><br/>live in minutes"]

  BRIEF --> BUILD --> CHECK --> READ --> SHIP
  SHIP -. "next piece" .-> BRIEF

  classDef human fill:#2a2415,stroke:#c9a227,stroke-width:3px,color:#f5f2ec
  classDef agent fill:#1a2028,stroke:#39424e,color:#f5f2ec
  class BRIEF,READ,SHIP human
  class BUILD,CHECK agent
```

::: {.pull style="margin-top:1.1em"}
Seven phases. Twenty-one trips round the loop.
:::

[Small pieces meant every mistake stayed small. And shipping took minutes rather than a release cycle — so problems showed up while the work was still fresh.]{.caption}

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:55 — TOTAL 9:00]</span>

Here's the working rhythm. Five steps, and it really is this simple.

<span style="color:#c1121f;font-weight:700">[TRACE THE DIAGRAM LEFT TO RIGHT AS YOU TALK]</span>

I describe one piece — never the whole thing. The AI builds it and writes the tests at the same time. Then the machine checks its own work automatically, which is where the cheap mistakes get caught, for free. Then I read it. Then it goes live.

<span style="color:#c1121f;font-weight:700">[POINT TO THE ARROW LOOPING BACK]</span>

And straight round again. Seven planned phases, twenty-one trips round the circuit in total — because every fix went round it too.

<span style="color:#c1121f;font-weight:700">[SLOW DOWN — THIS IS THE VALUE POINT]</span>

What matters here is the speed of that loop, not any single step in it.

Small pieces mean every mistake stays small. And because getting something in front of real data takes minutes rather than a release cycle, you find out you were wrong while the work is still fresh.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

That's the real productivity story — not "the AI types quickly", but the whole cycle collapsing from weeks into an evening.
:::

## The problem I didn't know I had

[The decision that makes it feel live]{.kicker}

```{python}
#| label: rate-mismatch

DURATION, TRADES_SEC = 3.0, 300
N = int(DURATION * TRADES_SEC)
t = np.sort(rng.uniform(0, DURATION, N))
price = 187.40 + np.cumsum(rng.normal(0, 0.006, N))

def coalesce(interval_ms):
    """The real rule: keep the latest price, send it at most once per window."""
    step = interval_ms / 1000.0
    xs, ys = [], []
    for e in np.arange(step, DURATION + step, step):
        taken = price[t <= e]
        if len(taken):
            xs.append(e); ys.append(taken[-1])
    return xs, ys

x250, y250 = coalesce(250)

def note(ms, n_out):
    """The chart's only annotation: headline, current setting, and the key."""
    return [dict(
        x=0.015, y=0.98, xref="paper", yref="paper",
        xanchor="left", yanchor="top", showarrow=False, align="left",
        font=dict(family="Bricolage Grotesque", size=30, color=INK),
        bgcolor="rgba(14,17,22,0.9)", bordercolor=BRASS, borderwidth=1, borderpad=14,
        text=(f"<b>{N} price updates in</b> &nbsp;→&nbsp; "
              f"<b style='color:{BRASS}'>{n_out} reach the screen</b>"
              f"<br><span style='font-size:20px;color:{MUTED}'>"
              f"sending at most once every {ms} ms &nbsp;·&nbsp; "
              f"<span style='color:{MUTED}'>grey = every trade</span>, "
              f"<span style='color:{BRASS}'>gold = your screen</span></span>"))]

fig = go.Figure()
fig.add_trace(go.Scatter(
    x=t, y=price, mode="markers",
    marker=dict(size=4.5, color=MUTED, opacity=0.45),
    hovertemplate="a trade<br>$%{y:.2f}<extra></extra>"))
fig.add_trace(go.Scatter(
    x=x250, y=y250, mode="lines+markers",
    line=dict(color=BRASS, width=3.5, shape="hv"),
    marker=dict(size=12, color=BRASS, line=dict(color=PAPER, width=2)),
    hovertemplate="what the screen gets<br>$%{y:.2f}<extra></extra>"))

buttons = []
for ms in (100, 250, 1000):
    xs, ys = coalesce(ms)
    buttons.append(dict(label=f"  every {ms} ms  ", method="update",
        args=[{"x": [t, xs], "y": [price, ys]},
              {"annotations": note(ms, len(xs))}]))

fig.update_layout(
    height=530, autosize=True,
    annotations=note(250, len(x250)),
    xaxis=dict(title=dict(text="three seconds of one stock",
                          font=dict(size=19, color=MUTED)),
               range=[0, DURATION], showticklabels=False, showgrid=False),
    yaxis=dict(visible=False),
    updatemenus=[menu_style(buttons=buttons, active=1)],
    margin=dict(l=30, r=30, t=70, b=56),
    hovermode="closest")
show(fig)
```

[Illustrative of the ratio described in the project's guide, not a recording of real trades.]{.caption}

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 1:25 — TOTAL 10:25]</span>

This is the moment in the project that surprised me most.

A popular stock like Apple has hundreds of trades every second.

<span style="color:#c1121f;font-weight:700">[POINT TO CHART — THE GREY CLOUD]</span>

That grey cloud is three seconds of it. Nine hundred price updates.

Now — your screen only physically redraws about sixty times a second. And every one of those updates makes the browser recalculate and repaint the page, on the same single thread that handles your clicks. Three hundred a second is far more than it can get through, so the work piles up and the tab stops responding altogether.

<span style="color:#c1121f;font-weight:700">[PAUSE — THEN SAY THIS PLAINLY, IT IS THE POINT OF THE SLIDE]</span>

I did not know that. It never occurred to me.

I asked for live prices. What came back was live prices *and* a fix for a problem I hadn't thought about, with the reasoning written out next to it.

<span style="color:#c1121f;font-weight:700">[POINT TO THE GOLD STAIRCASE]</span>

That's the gold line. Keep only the newest price, and send it four times a second. Nine hundred in, twelve out — and your eye simply cannot tell.

<span style="color:#c1121f;font-weight:700">[CLICK "every 100 ms"]</span> More often tracks the market closer, but quadruples the load on every browser.

<span style="color:#c1121f;font-weight:700">[CLICK "every 1000 ms"]</span> Once a second is cheap — but you can see it lagging.

<span style="color:#c1121f;font-weight:700">[CLICK BACK TO "every 250 ms"]</span> A quarter of a second is what shipped.

<span style="color:#c1121f;font-weight:700">[SLOW DOWN — THIS IS THE VALUE MESSAGE]</span>

On my own, I'd have wired the feed straight to the screen and watched it fall over on the first busy stock, with no idea why. Instead I got the version an experienced engineer would have built, first time.

It isn't just that it types faster than you. It brings knowledge you don't have — and explains it well enough that you can check it.
:::

## Four ways a bug turned up

[Because things still go wrong — the question is how fast you find out]{.kicker}

::: {.cols}
::: {.card}
[Automatically]{.tag}[The tests]{.h4}

291 of them, written alongside the code and run on every single change. Free, instant, and they never get bored.
:::
::: {.card}
[By reading]{.tag}[A leak every test missed]{.h4}

Something that quietly grew forever. All 291 tests passed — tests check that answers are right, not that memory behaves.
:::
:::

::: {.cols style="margin-top:0.7em"}
::: {.card}
[In production]{.tag}[A data plan that fell short]{.h4}

Only visible once real requests hit a real market. Fixed by adding a second supplier rather than paying to upgrade — **a one-file change**.
:::
::: {.card}
[By looking at it]{.tag}[A chart that died on screen]{.h4}

Nothing was broken except what you could see. It took a screenshot to catch it.
:::
:::

::: {style="margin-top:1em;font-size:0.54em;line-height:1.5;color:#8b929c;max-width:40em"}
Three of the four only appear **after** it is live — which is exactly why shipping in minutes, not weeks, is worth so much.
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 1:00 — TOTAL 11:25]</span>

It'd be a disservice to show only the parts that went well. Four real problems, each caught a different way.

<span style="color:#c1121f;font-weight:700">[POINT TO TOP-LEFT]</span>

The cheap one — the automated tests. Two hundred and ninety-one, running on every change. Instant and free.

<span style="color:#c1121f;font-weight:700">[POINT TO TOP-RIGHT]</span>

The second is more interesting. Something was quietly growing and never cleaning up after itself. Every test passed — tests check that answers are correct, not that something is slowly eating the machine. It took reading the code to catch that.

<span style="color:#c1121f;font-weight:700">[POINT TO BOTTOM-LEFT]</span>

The third only appeared in the real world: the free data plan didn't include historical prices. You can't discover that on a laptop.

<span style="color:#c1121f;font-weight:700">[EMPHASISE — IT'S THE GOOD BIT]</span>

And the fix was a second supplier rather than paying to upgrade — one file, because of a structural decision made days earlier.

<span style="color:#c1121f;font-weight:700">[POINT TO BOTTOM-RIGHT]</span>

The fourth: a chart that simply didn't draw. It took someone looking at the screen.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

Three of those four only turned up after it was live. Which is why the speed of that loop matters — you find out almost immediately, and fixing it is a correction, not a project.
:::

## Side by side

[The same application, two ways of building it]{.kicker}

<div class="scorecard">
  <span class="sc-h"></span>
  <span class="sc-h">The traditional way</span>
  <span class="sc-h new">With an AI partner</span>
  <span class="sc-k">Time</span>
  <span class="sc-v">68–92 hours<small>the plan's own estimate</small></span>
  <span class="sc-v new">9 hours<small>measured</small></span>
  <span class="sc-k">People</span>
  <span class="sc-v">5–6 specialisms<small>a small team, and their calendars</small></span>
  <span class="sc-v new">One person<small>directing the work</small></span>
  <span class="sc-k">Technologies</span>
  <span class="sc-v">18<small>each one learned first</small></span>
  <span class="sc-v new">The same 18<small>used without mastering them first</small></span>
  <span class="sc-k">Writing the code</span>
  <span class="sc-v">~11,000 lines, by hand</span>
  <span class="sc-v new">Described, generated, reviewed</span>
  <span class="sc-k">Iterating</span>
  <span class="sc-v">Scheduled into a release</span>
  <span class="sc-v new">One piece per sitting</span>
  <span class="sc-k">Finding the bugs</span>
  <span class="sc-v">Still the real work</span>
  <span class="sc-v new">Still the real work</span>
  <span class="sc-k">Cost to build</span>
  <span class="sc-v">Thousands, in engineering time</span>
  <span class="sc-v new">A subscription already paid for</span>
</div>


[Left column: the project's own written estimate, made before the build. Right column: measured from the finished project. The bottom three rows are qualitative comparisons, not measurements.]{.source}

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 1:15 — TOTAL 12:40]</span>

<span style="color:#c1121f;font-weight:700">[DO NOT READ EVERY ROW ALOUD — WALK DOWN THE RIGHT-HAND COLUMN AND LET THEM READ THE REST]</span>

The whole thing in one place. Same application, two ways of getting there. Grey is how this would normally go; gold is what happened.

<span style="color:#c1121f;font-weight:700">[POINT TO THE TIME ROW]</span>

Time we've covered. Sixty-eight to ninety-two hours against nine.

<span style="color:#c1121f;font-weight:700">[POINT TO THE PEOPLE ROW]</span>

People. Five or six specialisms — and that's not just headcount, it's five or six calendars that have to line up.

<span style="color:#c1121f;font-weight:700">[POINT TO THE TECHNOLOGIES ROW — SLOW DOWN, THIS IS THE MOST IMPORTANT ROW]</span>

This row is the one I find most interesting, and it's easy to misread.

The number of technologies didn't go down. It's still eighteen. The app is exactly as complicated as it always was.

What changed is that you no longer have to have mastered all eighteen before you're allowed to start. You still have to know what you're asking for, and recognise a bad answer. But the entry fee dropped enormously.

<span style="color:#c1121f;font-weight:700">[POINT TO THE "FINDING THE BUGS" ROW]</span>

And I've deliberately left this row identical on both sides, because it is.

Finding problems is still the real work. Generating code got faster; checking whether it's right did not. Anyone who tells you otherwise is selling something.

<span style="color:#c1121f;font-weight:700">[PAUSE]</span>

And cost — which is worth its own slide.
:::

## What it cost

[Pick whichever day rate is realistic for you]{.kicker}

::: {.split}
::: {}
```{python}
#| label: cost

RATES = [400, 600, 800]
def cost_bars(rate):
    return [PLAN_LO * rate, PLAN_MID * rate, PLAN_HI * rate]

labels = ["Low estimate", "Midpoint", "High estimate"]

fig = go.Figure(go.Bar(
    x=labels, y=cost_bars(RATES[1]),
    marker=dict(color=MUTED, line=dict(color=PAPER, width=1)),
    text=[f"${v:,.0f}" for v in cost_bars(RATES[1])],
    textposition="outside", cliponaxis=False,
    textfont=dict(family="Bricolage Grotesque", size=30, color=INK),
    hovertemplate="%{x}<br>$%{y:,.0f}<extra></extra>"))

# The active button is not highlighted (see menu_style), so the chart says
# which rate it is currently showing.
def rate_note(rate):
    return [dict(x=1.0, y=1.19, xref="paper", yref="paper",
                 xanchor="right", showarrow=False,
                 font=dict(family="IBM Plex Mono, monospace", size=20, color=BRASS),
                 text=f"at ${rate} per developer-day")]

fig.update_layout(
    height=470, autosize=True, bargap=0.45,
    annotations=rate_note(RATES[1]),
    yaxis=dict(visible=False, range=[0, 11800]),
    xaxis=dict(showgrid=False, zeroline=False,
               tickfont=dict(family="Instrument Sans", size=22, color=MUTED)),
    margin=dict(l=20, r=20, t=96, b=64),
    updatemenus=[menu_style(active=1, buttons=[
        dict(label=f"  ${r}/day  ", method="update",
             args=[{"y": [cost_bars(r)],
                    "text": [[f"${v:,.0f}" for v in cost_bars(r)]]},
                   {"annotations": rate_note(r)}])
        for r in RATES])])
show(fig)
```

[What the plan's own estimate is worth in engineering time. No salary figure is asserted — the buttons are yours to set.]{.caption}
:::

::: {}
::: {style="padding-top:0.3em"}
[About]{.pre}
[$3]{.hero}

[**a month to host it.** One always-on machine and the disk the records live on — a real invoice, not an estimate.]{.hero-note}
:::

::: {.figrow .n-2 style="margin-top:1.4em"}
::: {}
[Flat]{.pre}[$0]{.fig-n}[market data · two free plans]{.fig-t}
:::
::: {}
[Under]{.pre}[$10]{.fig-n}[typical all-in total a month]{.fig-t}
:::
:::
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:50 — TOTAL 13:30]</span>

I'll be careful here too, because I don't know your loaded day rate.

<span style="color:#c1121f;font-weight:700">[POINT TO CHART]</span>

This takes the plan's own estimate and multiplies it by a rate. At six hundred a day, ten developer-days is around six thousand dollars of engineering time.

<span style="color:#c1121f;font-weight:700">[CLICK THROUGH THE RATE BUTTONS: $400, THEN $800, THEN BACK TO $600]</span>

Slide it to whatever's realistic for you. The exact figure doesn't matter — the order of magnitude does.

<span style="color:#c1121f;font-weight:700">[TURN TO THE RIGHT-HAND SIDE]</span>

Against that: about three dollars a month to host. That's an actual invoice. Nothing for the market data. With the AI usage on top, under ten dollars a month all in.

<span style="color:#c1121f;font-weight:700">[PAUSE, THEN SLOW DOWN — THIS IS THE EXECUTIVE TAKEAWAY]</span>

And this is what actually matters for an organisation. It isn't that engineering got cheaper. It's that the cost of *trying something* collapsed.

An idea that isn't worth two weeks of a team's time — and most aren't — might well be worth an afternoon.
:::

## Including this deck

[The same loop, pointed at the project itself]{.kicker}

::: {.steps .n-3}
::: {.step}
[Step 1]{.sn}[Read the project]{.st}[Its history, its timestamps, its own written plan.]{.sd}
:::
::: {.step}
[Step 2]{.sn}[Draw the evidence]{.st}[Every chart here is generated from that data, not drawn by hand.]{.sd}
:::
::: {.step}
[Step 3]{.sn}[Cut what can't be traced]{.st}[Any number without a source in the project came out.]{.sd}
:::
:::

::: {.split style="margin-top:1.2em"}
::: {}
::: {.pull}
Change the project, re-render, and these slides change with it.
:::
:::
::: {}
::: {style="text-align:right"}
[half a day]{.hero .sm}
[to build this presentation]{.hero-note}
:::
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:30 — TOTAL 14:00]</span>

One more thing, and it's slightly recursive. This presentation was built the same way.

<span style="color:#c1121f;font-weight:700">[POINT ALONG THE THREE STEPS]</span>

The same tool read the project — the history, the plan, the numbers — and generated these charts as code, from that data.

Then the same discipline: anything I couldn't trace back to a real file came out. There was a first draft of that cost slide with an invented salary in it. That's why you got buttons instead.

<span style="color:#c1121f;font-weight:700">[POINT TO "HALF A DAY"]</span>

The whole deck took about half a day.
:::

## What actually changed {.center}

::: {.pull}
The distance between having an idea and having a working thing just got dramatically shorter.
:::

::: {.takeaways style="margin-top:1.1em;max-width:38em"}
[A prototype now costs **hours**, not a release cycle.]{.take}
[You can build across technologies you have **not** mastered.]{.take}
[Being wrong got **cheap** — so you can afford to try far more things.]{.take}
[The scarce skill moved from writing it to **knowing what to build**.]{.take}
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:45 — TOTAL 14:45]</span>

<span style="color:#c1121f;font-weight:700">[SLOW DOWN. THIS IS THE CLOSE — LET EACH LINE BREATHE.]</span>

Four things I'd take away.

<span style="color:#c1121f;font-weight:700">[POINT TO EACH LINE AS YOU SAY IT]</span>

A prototype now costs hours rather than a release cycle — so "let's just try it" becomes a reasonable answer in a meeting.

You can build across technologies you haven't mastered. You still need to know what good looks like; you no longer need years in each one first.

Being wrong got cheap. If an attempt costs an afternoon, you can afford for most of them to fail.

And the scarce skill moved — from writing the thing, to knowing what's worth building and telling whether what came back is any good.

<span style="color:#c1121f;font-weight:700">[PAUSE. THEN READ THE QUOTE AT THE TOP, SLOWLY.]</span>

So if you take one sentence out of this room, make it that one.
:::

##  {#thanks .center}

::: {style="max-width:30em"}
[Questions welcome]{.kicker}

::: {style="font-family:'Bricolage Grotesque',sans-serif;font-weight:700;font-size:3.8em;line-height:1;letter-spacing:-0.04em;margin:0.1em 0 0 0"}
Thank you
:::

::: {style="margin-top:0.9em;font-size:0.56em;line-height:1.6;color:#8b929c;max-width:22em"}
Nine hours, three dollars a month, and a thing that actually runs.
:::

::: {.cols .cols-3 style="margin-top:1.4em"}
::: {}
[The app]{.kicker}
[ticker-dash.fly.dev](https://ticker-dash.fly.dev/){target="_blank" rel="noopener" style="font-family:'IBM Plex Mono',monospace;font-size:0.42em"}
:::
::: {}
[The code]{.kicker}
[github.com/IHoKo/<br>Live-Trading-Dashboard](https://github.com/IHoKo/Live-Trading-Dashboard){target="_blank" rel="noopener" style="font-family:'IBM Plex Mono',monospace;font-size:0.42em"}
:::
::: {}
[These slides]{.kicker}
[ihoko.github.io/<br>Quarto_Presentation](https://ihoko.github.io/Quarto_Presentation/){target="_blank" rel="noopener" style="font-family:'IBM Plex Mono',monospace;font-size:0.42em"}
:::
:::
:::

::: {.notes}
<span style="color:#c1121f;font-weight:700">[TARGET TIME: 0:15 — TOTAL 15:00]</span>

Thank you.

<span style="color:#c1121f;font-weight:700">[PAUSE — 2 SECONDS. DO NOT RUSH INTO QUESTIONS.]</span>

Everything's up there — the app is live, the code is public, and these slides include all the working, so you can check any number I've given you.

<span style="color:#c1121f;font-weight:700">[LEAVE THIS SLIDE UP FOR Q&A. PRESS LEFT ARROW TO GET BACK TO THE LIVE APP IF YOU NEED IT.]</span>

<span style="color:#c1121f;font-weight:700">[PREPARED ANSWERS — DO NOT READ THESE ALOUD:]</span>

<span style="color:#c1121f;font-weight:700">[IF ASKED "COULD I DO THIS?" → YES. YOU NEED TO KNOW WHAT YOU WANT, AND BE ABLE TO TELL WHETHER WHAT COMES BACK IS RIGHT. YOU DO NOT NEED TO KNOW HOW TO BUILD IT.]</span>

<span style="color:#c1121f;font-weight:700">[IF ASKED ABOUT CODE QUALITY → 291 TESTS, AUTOMATED CHECKS ON EVERY CHANGE, AND A READ-THROUGH BEFORE ANYTHING SHIPPED. THEN POINT AT THE LEAK THE TESTS MISSED.]</span>

<span style="color:#c1121f;font-weight:700">[IF ASKED WHAT IT COST → ONE MONTHLY SUBSCRIPTION I PAY ANYWAY, PLUS ABOUT $3/MONTH TO HOST.]</span>

<span style="color:#c1121f;font-weight:700">[IF PUSHED ON THE 9x → THE MEASURED SIDE IS THE CONSERVATIVE END AND ROUNDED DOWN. THE ESTIMATE PREDATES THE BUILD AND WAS NEVER REVISED.]</span>

<span style="color:#c1121f;font-weight:700">[IF PUSHED HARDER — "THE TOOL ESTIMATED ITS OWN WORK" → CONCEDE IT IMMEDIATELY, IT IS A FAIR POINT. THEN: THE SIZING IS CONVENTIONAL, IT IS IN THE REPO TO BE READ, YOU HAVE ALL SEEN THE SCOPE, AND HALVING IT STILL LEAVES FOUR OR FIVE TIMES.]</span>

<span style="color:#c1121f;font-weight:700">[IF ASKED "DOES THIS REPLACE DEVELOPERS?" → NO. IT REMOVED THE TYPING, NOT THE ENGINEERING. THE BUGS SLIDE IS THE HONEST ANSWER — POINT BACK TO IT.]</span>

<span style="color:#c1121f;font-weight:700">[IF ASKED WHAT YOU'D DO DIFFERENTLY → SHIP EVEN EARLIER. THREE OF THE FOUR PROBLEMS ONLY APPEARED IN PRODUCTION.]</span>
:::

##  {#demo-backup visibility="uncounted"}

[Backup · the real dashboard, if the live one will not load]{.kicker}

![](figs/dashboard.png){.shot fig-alt="The Ticker dashboard: a seven-symbol ticker tape, portfolio summary tiles, an empty positions table, the AAPL price chart, and the analyst panel mid-answer."}

[Captured out of hours — the tape reads **CLOSED**, and an unavailable number renders as **—**, never as a zero.]{.caption}

::: {.notes}
<span style="color:#c1121f;font-weight:700">[UNCOUNTED SLIDE — ONLY USE THIS IF THE LIVE APP WILL NOT LOAD.]</span>

This is the dashboard. Across the top is the ticker tape — seven symbols, each updating on its own when the market is open.

The tiles are the portfolio summary. On the right is the analyst panel, mid-answer, with its disclaimer underneath.

<span style="color:#c1121f;font-weight:700">[POINT TO THE DASH IN THE PROFIT AND LOSS COLUMN]</span>

And that dash is the rule I mentioned — where a number isn't available, it says so, rather than showing you a zero that looks real.
:::
